export function Hero() {
  return (
    <section className="border-b border-border">
      <div className="mx-auto max-w-6xl px-5 py-14 sm:px-8 sm:py-24">
        <h1 className="font-display max-w-3xl text-[2rem] font-medium leading-[1.15] tracking-tight text-foreground sm:text-5xl sm:leading-[1.12]">
          Build a complete interior direction in seconds.
        </h1>
        <p className="mt-5 max-w-2xl text-[15px] leading-relaxed text-muted sm:mt-6 sm:text-lg">
          Select a space, style and mood. The generator assembles a color
          palette, material mix, furniture direction, lighting strategy and a
          ready-to-use AI render prompt — entirely offline.
        </p>
      </div>
    </section>
  );
}
